Capito. Al momento, con lo strumento **Repository** che hai indicato, riesco ad ottenere solo i **metadati** della repository (URL, branch, license, ecc.) e **non il contenuto testuale dei file** (quindi non posso leggere né `README.md` né la sezione `scripts` di `package.json`).

✅ Per procedere ho bisogno di una delle due alternative:

- **A)** Mi abiliti un **tool/endpoint aggiuntivo** per leggere il contenuto dei file (es. “Get file content” per `README.md` e `package.json`).
- **B)** Incolli tu qui il contenuto attuale di `README.md` e/o della sezione `"scripts"` di `package.json` (anche parziali). Poi li revisiono subito e ti preparo la versione migliorata pronta da sostituire.

Dimmi quale scegli (**A** o **B**).