Ho provato a usare lo strumento **Repository**, ma al momento riesco a ottenere solo **metadati del repo** (nome, lingua JavaScript, licenza MIT, branch, ecc.) e **non il contenuto dei file**. Di conseguenza, **non posso leggere/modificare direttamente** `README.md` né vedere `package.json`, quindi non posso ricavare i comandi per **Build/Run**.

Per procedere come richiesto, incollami qui:

1) **Tutto il contenuto di `README.md`**
2) La sezione **`"scripts"`** di `package.json` (anche solo quella)

In più dimmi la preferenza: **npm / yarn / pnpm** (oppure “va bene qualsiasi”).

Appena mi incolli quei contenuti ti preparo:
- una **versione migliorata** del README
- una sezione **Build/Run** con i **comandi corretti** basati su `scripts`

Vuoi incollare prima **README.md** o prima **scripts**?