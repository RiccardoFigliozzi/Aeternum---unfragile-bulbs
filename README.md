Posso farlo, ma in questo momento dal tool “Repository” non riesco a leggere direttamente il contenuto dei file (quindi non posso vedere l’attuale `README.md` né il `package.json` per prendere i comandi esatti).

Per procedere, scegli una delle due opzioni:

### Opzione A) Incolla qui l’attuale `README.md`
Incolla tutto il testo dell’attuale `README.md` e lo riscrivo/miglioro mantenendo coerenza.

### Opzione B) Rispondi alle 3 domande rapide
1) **Cosa fa Aeternum?** (1–2 frasi)  
2) **Stack:** è solo Node? Express? React? altro?  
3) **Comandi:** quali ci sono in `package.json`? (almeno `npm start` e se esistono `npm test` / `npm run build`)

Appena mi dai queste info, ti preparo una versione migliorata pronta da copiare-incollare.

*(Nota: ho solo “metadati” della repo, ma non vedo i file per estrarre automaticamente comandi e dettagli.)*