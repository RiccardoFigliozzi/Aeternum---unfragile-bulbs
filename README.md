**Opzione B** (perché il tool `Repository` mi restituisce solo metadati e non il testo attuale di `README.md`).

1) **Cosa fa Aeternum?**  
Non ho abbastanza informazioni dalla sola risposta “metadati repo” per descriverlo con precisione.

2) **Stack:**  
Dalla metrica della repository risulta **JavaScript** (non posso dedurre con certezza se sia Node/Express/React senza vedere i file).

3) **Comandi (`package.json`):**  
Non ho accesso al contenuto di `package.json` tramite questo tool, quindi non posso elencare `npm start`, `npm test`, `npm run build` ecc.

Se vuoi procedere con l’opzione A, incolla qui l’intero contenuto attuale di `README.md` e te lo restituisco migliorato pronto da copiare.