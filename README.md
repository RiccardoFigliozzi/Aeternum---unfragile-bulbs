Non riesco ancora a leggere/recuperare il contenuto di **README.md** né la sezione **`"scripts"`** di `package.json`: lo strumento che posso usare in questa chat mi sta restituendo solo metadati della repository (nome license permessi ecc.), non i file testuali.

Per sbloccarti subito: per favore **incolla qui**:
1) **contenuto integrale di `README.md`**
2) **solo** la sezione `"scripts"` di `package.json` (in formato esatto tipo ` "scripts": { ... } `)

Appena me li dai, ti restituisco un **README.md completo e pronto da copiare/incollare** (migliorato, chiaro e strutturato).