Perfetto—posso migliorare `README.md`, **ma per farlo mi servono i contenuti**.

Al momento il tool `Repository` che ho a disposizione mi restituisce solo i metadati della repo (non il testo dei file), quindi **non posso leggere automaticamente**:

1) il contenuto attuale di `README.md`
2) la sezione `"scripts": { ... }` dentro `package.json`

👉 Incolla qui **esattamente** (come hai chiesto tu):
1. **Tutto il contenuto attuale di `README.md`**
2. **Solo** la sezione `"scripts": { ... }` di `package.json` in questo formato:

```json
"scripts": { ... }
```

Appena me li mandi, ti restituisco:
- il **README.md completo migliorato** (pronto da copiare/incollare)
- una sezione **Build/Run** perfettamente coerente al 100% con gli `scripts` che mi fornisci.