Non riesco ancora a leggere il contenuto dei file della repo con lo strumento **Repository**: mi sta restituendo solo metadati (nome, licenza, permessi, ecc.) e non il testo di `README.md` né la sezione `"scripts"` di `package.json`.

Per prepararti una versione migliorata “pronta da copiare”, incollami qui **tu** (come avevi chiesto):

1) **contenuto integrale attuale di `README.md`**  
2) **sezione `"scripts"` di `package.json`** (anche solo quella)

Appena me li incolli, ti restituisco direttamente un nuovo `README.md` completo e migliorato.