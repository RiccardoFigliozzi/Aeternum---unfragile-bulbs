Non riesco ad ottenere il contenuto dei file (es. `README.md` e `package.json`) tramite lo strumento **Repository**: al momento posso solo vedere metadati della repo (owner, branch, licenza, ecc.), non i contenuti testuali.

Per procedere come chiedi, incollami qui:

1) **Il contenuto completo attuale** di `README.md`  
2) **Solo** la sezione:
```json
"scripts": { ... }
```

Appena li incolli, ti preparo subito una versione **migliorata** del README e una sezione **Build/Run** coerente **con i comandi esatti** presenti in `package.json`.