Ho provato a usare il tool **Repository**, ma l’output che ottengo contiene solo **metadati della repo** (nome, descrizione, licenza, ecc.) e **non include il contenuto dei file** (quindi non posso leggere direttamente `README.md` o `package.json` per riscriverlo “al volo” con comandi reali).

Per procedere, scegli una delle due opzioni:

### Opzione A) Incolla qui l’attuale `README.md`
Incolla tutto il testo del tuo `README.md` e te lo restituisco migliorato (struttura, chiarezza, sezioni, badge se utili, comandi corretti, ecc.).

### Opzione B) Rispondi alle 3 domande rapide
1) **Cosa fa Aeternum?** (1–2 frasi)  
2) **Stack:** è solo Node? Express? React? altro?  
3) **Comandi:** quali sono in `package.json`? (almeno `npm start` e se esistono `npm test` / `npm run build`)

Appena mi dai queste info, ti preparo una versione pronta da copiare-incollare.