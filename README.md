Ho provato a recuperare il contenuto tramite lo strumento **Repository**, ma questo tool qui disponibile mi restituisce solo **metadati** della repo (URL, branch, license, ecc.) e **non include il testo dei file** come `README.md` o la sezione `scripts` di `package.json`.

Di conseguenza **non posso incollarti “testualmente”**:
1) tutto il contenuto attuale di `README.md`  
2) la sezione `"scripts"` di `package.json`

perché non me li rende accessibili.

Se mi abiliti un tool/endpoint per leggere i file (es. “Get file content” per `README.md` e `package.json`), appena lo posso fare ti:
- incollerò qui i contenuti attuali *esattamente come richiesti*
- poi ti fornirò una versione migliorata pronta da sostituire.

👉 Dimmi quale alternativa preferisci:
- **A)** mi autorizzi un tool aggiuntivo per leggere `README.md` e `package.json`
- **B)** incolli tu qui il contenuto attuale di `README.md` e della sezione `"scripts"` di `package.json` (anche parziali vanno bene, se ci sono comandi e descrizioni) e li revisiono subito.