Ho provato a recuperare i contenuti del repository con lo strumento `Repository`, ma al momento riesco a ottenere solo **metadati** (es. nome, branch, licenza, ecc.) e **non il testo** di `README.md` né di `package.json` (quindi non posso riscriverlo partendo dall’attuale contenuto).

Per procedere, dimmi la tua preferenza di lingua: **Italiano** o **Inglese**.

Poi scegli uno dei due modi:

1) **Incollami qui**:
   - il contenuto completo attuale di `README.md`
   - la sezione `"scripts"` di `package.json` (testo esatto)
   - (opzionale) obiettivo del progetto / stack e come si usa

2) Oppure incolla qui (anche parzialmente) il testo usando “**View raw**” su GitHub.

Appena mi incolli `README.md` attuale + `"scripts"` di `package.json`, ti restituisco un **README.md completamente riscritto e pronto da sostituire**.