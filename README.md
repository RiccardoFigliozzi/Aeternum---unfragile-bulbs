Ho controllato con il tool `Repository`: però la risposta che ricevo contiene **solo i metadati della repository** (non l’albero dei file e non il contenuto dei file). Quindi, in questo momento, **non posso leggere né copiare** `README.md` o `package.json`.

Per migliorare davvero il README ho bisogno di uno di questi accessi aggiuntivi:

1) Un tool/end-point per leggere il contenuto di file (es. `contents/README.md` oppure “Get file content / View raw file”).  
2) Un tool per elencare i file della repo (tree) e poi ottenere il singolo file con un secondo step.  
3) In alternativa: il contenuto di `README.md` e di `package.json` incollato qui dall’utente.

### Domande rapide (per sbloccarmi)
1) Preferisci che il README finale sia in **Italiano** o **Inglese**?  
2) Vuoi un approccio “pronto da copiare” (ti restituisco direttamente il nuovo testo di `README.md`) oppure ti propongo prima una bozza/struttura?

### Cosa mi serve da te adesso (scegli una opzione)
- **Opzione A (consigliata):** abilita/fornisci un tool tipo **Get file content / View raw** e dimmi come si chiama esattamente.  
- **Opzione B:** incolla qui il contenuto attuale di `README.md` (anche parziale) e la sezione `"scripts"` di `package.json` (basta quella).

Appena ho il testo di `README.md` (e idealmente `"scripts"`), preparo una versione migliorata e coerente con come si avvia/builda il progetto.