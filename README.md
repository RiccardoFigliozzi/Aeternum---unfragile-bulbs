Ho provato a usare lo strumento **Repository**, ma al momento mi sta restituendo solo **metadati del repo** (nome, lingua JavaScript, licenza MIT, branch, ecc.) e **non il contenuto dei file**—quindi **non posso leggere/modificare direttamente** `README.md` né vedere `package.json` (quindi non posso ricavare i comandi per Build/Run).

Per procedere come richiesto, mi servono i contenuti. Nel modo più veloce:

1) **Incolla qui tutto il `README.md`**  
2) **Incolla qui la sezione `"scripts"` di `package.json`** (anche solo quella)

In più dimmi la preferenza: **npm / yarn / pnpm** (oppure “va bene qualsiasi”).

Appena mi incolli quei contenuti ti restituisco:
- una **versione migliorata del README**
- una sezione **Build/Run** con i **comandi corretti** basati su `scripts`.

Vuoi procedere incollando prima **README.md** o prima **scripts**?