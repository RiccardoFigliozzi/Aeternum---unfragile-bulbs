Ecco una versione **migliorata e più utile** del `README.md` (puoi sostituire l’attuale contenuto con questo). Non avendo accesso ai file `package.json` e all’albero della repo tramite gli strumenti disponibili, ho scritto una struttura “pronta” con sezioni da completare facilmente.

```md
# Aeternum — Unfragile Bulbs

Progetto (web / JavaScript).  
Repo: **RiccardoFigliozzi/Aeternum---unfragile-bulbs**

## Descrizione
Breve descrizione del progetto:
- Di cosa tratta
- Qual è l’obiettivo
- A cosa serve / a chi è rivolto

> 🔧 Nota: al momento non ho accesso al contenuto di `package.json` e all’albero dei file, quindi non posso riportare informazioni tecniche precise. Puoi integrare i dettagli sotto (Framework, comandi di build, ecc.).

## Prerequisiti
- Node.js (versione consigliata: `>= 18`)
- npm (o yarn/pnpm)

## Installazione
```bash
npm install
```

## Avvio in sviluppo
```bash
npm run dev
# oppure, se presente:
npm start
```

## Build (produzione)
```bash
npm run build
```

## Test
```bash
npm test
```

## Struttura del progetto
Esempio (da adattare):
- `src/` — sorgenti
- `public/` — asset statici
- `tests/` o `__tests__/` — test
- file di configurazione (es. `vite.config.*`, `next.config.*`, ecc.)

> Se vuoi, incolla qui l’output di:
> `tree -L 2`
> così posso generare una struttura aggiornata e completa.

## Licenza
Distribuito con **MIT License**.

## Contribuire
Contributi benvenuti! Apri una **issue** o una **pull request**.

## Contatti
- Autore: RiccardoFigliozzi
```

Se mi incolli **(1)** il contenuto di `package.json` e **(2)** l’output di `tree -L 2`, posso trasformare questo README in uno **specifico e accurato** (comandi esatti, descrizione più realistica, struttura precisa, ecc.).