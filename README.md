Non riesco a migliorare concretamente **README.md** in modo “coerente con gli `scripts`” perché, con lo strumento disponibile qui (“Repository”), riesco a ottenere **solo metadati** della repository (nome, branch, license, ecc.), ma **non il contenuto dei file**.

Quindi non posso recuperare:
- il testo attuale di `README.md`
- la sezione `"scripts": { ... }` di `package.json`

### Cosa posso fare subito
Se mi incolli qui:
1) **il contenuto attuale di `README.md`** (anche tutto)
2) **solo** la sezione `"scripts": { ... }` di `package.json`

io ti preparo immediatamente un **README.md completo migliorato** pronto da copiare/incollare, con una sezione **Build/Run** perfettamente allineata ai comandi reali definiti in `package.json`.

### Alternative (se vuoi procedere senza incollarli)
Dimmi se nel tuo ambiente hai un tool diverso (es. “read file”, “contents_url”, download file, ecc.). In tal caso posso ri-pianificare l’intervento.

Incollami pure i due estratti (README + scripts) e parto.